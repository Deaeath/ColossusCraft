package adris.altoclef.util.progresscheck;

public class DistanceProgressChecker extends LinearProgressChecker {
    public DistanceProgressChecker(double timeoutSeconds, double minProgress) {
        super(timeoutSeconds, minProgress);
    }
}
